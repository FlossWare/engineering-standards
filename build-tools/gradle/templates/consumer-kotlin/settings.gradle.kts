pluginManagement {
    repositories {
        maven {
            url = uri("https://packagecloud.io/flossware/releases/maven2/")
        }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "{PROJECT_NAME}"
